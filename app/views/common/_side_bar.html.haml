.chat-side
  .chat-user
    %h3.chat-user__name
      = current_user.name
    %ul.chat-user__icon
      %li.chat-user__icon__left
        = link_to new_group_path do
          = fa_icon 'pencil-square-o', class: 'chat-user__icon--group'
      %li.chat-user__icon__right
        = link_to edit_user_path(current_user) do
          = fa_icon 'cog', class: "chat-user__icon--user"
  .chat-group
    - current_user.groups.each do |group|
      = link_to group_messages_path(group) do
        .chat-group__name
          = group.name
        .chat-group__message
          メッセージはまだありません。
